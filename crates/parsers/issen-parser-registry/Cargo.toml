[package]
name = "issen-parser-registry"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
issen-core = { workspace = true }
issen-plugin-sdk = { workspace = true }
winreg-core = { workspace = true }
winreg-artifacts = { workspace = true }
# Targeted 0.7 dep (workspace is still on 0.5 — fleet not yet migrated): this
# crate uses only the free `services::is_known_service_binary` /
# `services::is_known_service_dll` fns, no forensicnomicon report/Observation
# traits, so the two coexisting versions never cross types.
forensicnomicon = "0.7"
anyhow = { workspace = true }
chrono = { workspace = true }
inventory = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
