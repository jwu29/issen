[package]
name = "issen-parser-prefetch"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
anyhow = { workspace = true }
issen-core = { path = "../../issen-core" }
# Decode prefetch (MAM/Xpress-Huffman + SCCA v30/31) via OUR published fleet
# crates — the reader (`prefetch-core`, for the full loaded-file list) plus the
# analyzer (`prefetch-forensic`, for graded anomalies).
prefetch-core = "0.1"
prefetch-forensic = "0.2"
forensicnomicon = { workspace = true }
chrono = { workspace = true }
serde_json = { workspace = true }
inventory = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
