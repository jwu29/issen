[package]
name = "issen-parser-lsasecrets"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
issen-core = { workspace = true }
issen-plugin-sdk = { workspace = true }
# LSA secrets (SECURITY\Policy\Secrets) — decode via OUR winreg-forensic fleet
# crates (winreg-artifacts::lsadump::parse_secrets over winreg-core), never
# third-party notatin. Names + sizes only; values are NOT decrypted (offline).
winreg-core = { workspace = true }
winreg-artifacts = { workspace = true }
anyhow = { workspace = true }
inventory = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
