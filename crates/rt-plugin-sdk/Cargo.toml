[package]
name = "rt-plugin-sdk"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license = "Apache-2.0"
description = "Plugin development kit for RapidTriage parsers"
repository.workspace = true

[dependencies]
rt-core = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
